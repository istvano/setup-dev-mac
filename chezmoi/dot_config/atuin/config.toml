# Keep shell history local unless sync is explicitly configured by the user.
auto_sync = false
update_check = false

# Insert selected history for review instead of executing it immediately.
enter_accept = false

[daemon]
enabled = false
autostart = false

[ai]
enabled = false
