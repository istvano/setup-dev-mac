# Ghostty terminal configuration.
# Reference: https://ghostty.org/docs/config

font-family = JetBrainsMono Nerd Font
font-size = 14

theme = dark:catppuccin-mocha,light:catppuccin-latte
window-padding-x = 8
window-padding-y = 6
window-save-state = never

# Scrollback stays in memory only. Terminal history on a workstation that
# handles credentials should not be written to disk by the emulator; Atuin owns
# deliberate command history instead.
scrollback-limit = 10000

copy-on-select = false
confirm-close-surface = true
shell-integration = zsh
shell-integration-features = cursor,sudo,title

macos-option-as-alt = true
macos-titlebar-style = tabs
clipboard-read = ask
clipboard-write = allow
clipboard-paste-protection = true
