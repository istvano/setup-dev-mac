# Native MLX environment

The initial environment contains MLX, MLX-LM, Hugging Face Hub and JupyterLab.
Ollama, llama.cpp and PyTorch are intentionally absent. `mlxctl serve` binds to
`127.0.0.1` and is for local development only.
