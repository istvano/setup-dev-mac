brew "awscli"  # AWS control-plane CLI kept on the host for interactive authenticated sessions.
brew "granted" # Issues short-lived, least-privilege SSO sessions and opens each account in its own browser profile.
