brew "step"         # Optional host CLI for explicit smallstep PKI workflows.
cask "burp-suite"   # Optional native GUI for interactive web security testing.
cask "knockknock"   # Optional native inspection of persistent macOS startup items.
cask "blockblock"   # Optional privileged monitor for new persistence mechanisms.
cask "oversight"    # Optional native monitor for microphone and camera activation.
cask "taskexplorer" # Optional native inspection of running macOS processes.
