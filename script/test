#!/usr/bin/env bash
set -Eeuo pipefail
ROOT="$(cd "$(dirname "${BASH_SOURCE[0]}")/.." && pwd)"
for test in shell-syntax render-brewfile yaml placement-policy idempotency; do bash "$ROOT/tests/$test.sh"; done
echo 'All repository tests passed.'
